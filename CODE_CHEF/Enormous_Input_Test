#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
    int n,k,c;
    cin>>n>>k;
    while(n--)
    {
        int t;
        cin>>t;
        if(t%k==0)
        {
            c++;
        }
    }
    cout<<c<<endl;
}
